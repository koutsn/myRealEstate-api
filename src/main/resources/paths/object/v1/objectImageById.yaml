get:
  summary: Image of Object per Id
  description: Image of Object per Id
  operationId: getObjectImageById
  tags:
    - Object
  security: [ ]
  parameters:
    - name: id
      in: path
      required: true
      description: id of object to delete
      schema:
        type: string
    - name: imageid
      in: path
      required: true
      description: id of object to delete
      schema:
        type: string
  responses:
    '200':
      description: Return image
      content:
        application/json:
          schema:
            $ref: ../../../schemas/object/v1/images.yaml

    '400':
      description: Bad request
      content:
        application/json:
          schema:
            $ref: ../../../schemas/error.yaml
